---
title: "R Markdown 数据报告"
author: "统计分析团队"
date: "`r Sys.Date()`"
output:
  html_document:
    toc: true
    toc_float: true
    theme: cosmo
    highlight: tango
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, message = FALSE, warning = FALSE)
```

## 📊 项目背景

本报告使用 R Markdown 展示统计分析流程，适合发布到 Posit Connect。

## 📦 加载库

```{r libraries}
library(ggplot2)
library(dplyr)
library(tidyr)
library(knitr)
```

## 📈 数据分析

### 数据生成

```{r data-generation}
set.seed(42)

df <- data.frame(
  x = 1:100,
  y = cumsum(rnorm(100)),
  category = sample(c('Alpha', 'Beta', 'Gamma'), 100, replace = TRUE)
)

head(df) %>% kable(caption = "数据预览")
```

### 描述性统计

```{r summary-stats}
df %>%
  group_by(category) %>%
  summarise(
    均值 = mean(y),
    标准差 = sd(y),
    最大值 = max(y),
    最小值 = min(y)
  ) %>%
  kable(digits = 2, caption = "分类统计摘要")
```

### 可视化

```{r plot, fig.width=10, fig.height=6}
ggplot(df, aes(x = x, y = y, color = category)) +
  geom_line(size = 1.2) +
  geom_point(size = 2, alpha = 0.6) +
  scale_color_manual(values = c('#00D9FF', '#FF6B6B', '#4ECDC4')) +
  theme_minimal() +
  labs(
    title = "时间序列数据可视化",
    subtitle = "按类别分组",
    x = "时间点",
    y = "累积值",
    color = "类别"
  ) +
  theme(
    plot.title = element_text(size = 16, face = "bold"),
    legend.position = "top"
  )
```

```{r boxplot, fig.width=8, fig.height=5}
ggplot(df, aes(x = category, y = y, fill = category)) +
  geom_boxplot(alpha = 0.7) +
  scale_fill_manual(values = c('#00D9FF', '#FF6B6B', '#4ECDC4')) +
  theme_minimal() +
  labs(
    title = "数据分布箱线图",
    x = "类别",
    y = "数值"
  ) +
  theme(legend.position = "none")
```

## 📝 结论

通过 R Markdown，我们可以：

- ✅ 整合代码、可视化和文本
- ✅ 生成可重现的分析报告
- ✅ 轻松发布到 Posit Connect

---

**部署说明**：将 `.Rmd` 文件推送到 Git 仓库，Posit Connect 将自动渲染并发布。
